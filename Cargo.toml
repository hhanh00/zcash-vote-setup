[package]
name = "zcash-vote-setup"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
figment = { version = "0.10", features = ["yaml"] }
serde = { version = "1.0", features = ["derive"] }
